<?xml version="1.0" encoding="utf-8"?>
<!--
  The API runs over plain HTTP during development, and Android blocks cleartext
  by default from API 28 up. Rather than switch cleartext on for the whole app,
  this permits it only for the loopback addresses a development API can live on:
  10.0.2.2 is the host as seen from the Android emulator, and localhost covers a
  port-forwarded device. Every other host — the real internet, PayPal included —
  still requires HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
